
  <%= form_with scope: :comment, url: micropost_comments_path(micropost.id), remote: true do |f| %>
    <div class="field">
      <%= f.text_area :content, placeholder: "Write your comment..." %>
    </div>
    <%= f.submit "Comment"%>
  <% end %>
